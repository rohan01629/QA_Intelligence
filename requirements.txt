# Runtime dependencies (mirror of pyproject.toml).
# Prefer: uv sync
# Or:     pip install -r requirements.txt

fastapi>=0.115.0
fastmcp>=2.0.0
httpx>=0.28.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
structlog>=24.4.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.0
